.. _deploy:

Deploying MqttKafkaBridge
=============================

You can deploy the Bridge by creating a docker image first.


1. Open a terminal and move to the root folder of the project
2. Run `docker build -t {image:tag} .`
3. Then push your image with `docker push`

